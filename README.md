```
pip install PyYAML markdown
```

usage:
```
  python md2book.py mybook.md -o mybook.html
```

After the book renders in the browser, type Ctrl+P to print. Be sure to check:

 [ ] Background Graphics
